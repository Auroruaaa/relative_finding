# Comparing PLINK and GERMLINE for Relative Detection

## Project Overview

In this project, we compare two commonly used tools for detecting related individuals in genetic data:

- **PLINK** (using `--genome`)
- **GERMLINE** (haplotype-based IBD segment detection)

The goal is to see how well each method can recover known relationships from the 1000 Genomes Project, using only chromosome 22 data.
The ground truth relationships are provided in:
`20140625_related_individuals.txt`


These include four relationships: Parent, Sibling, Trio, Second Order.
We extract those individuals from the chr22 VCF files and compare predicted relationships from PLINK and GERMLINE against the ground truth.

### Data

- chr22 VCF files (full + related samples)
- Ground truth relationship file
- Filtered subset VCF containing only GT individuals
- PLINK binary files (.bed/.bim/.fam)
- GERMLINE PED/MAP files

---

## What Have Been Done

### 1. Data Processing

- Parsed the ground truth file and extracted all related individuals.
- Located those individuals in the chr22 VCF files.
- Created a filtered VCF containing only those samples.
- Converted the data into PLINK format (.bed/.bim/.fam).
- Performed QC filtering and LD pruning (for PLINK analysis).

---

### 2. PLINK Analysis

We used plink --gemone and LD-pruned markers for unbiased allele-sharing estimation.

From PLINK output:
- Z0, Z1, Z2 extracted
- PI_HAT calculated
- Z1+Z2 used as alternative estimator

Relationship classification based on:

| Relationship   | Expected Z1+Z2 |
|---------------|----------------|
| First-degree  | >0.7           |
| Second-degree | >0.45          |
| Unrelated     | <=0.45         |

---

### 3. GERMLINE Implementations

We used the LD-pruned data generated by plink and delete the missing SNPs in the data. Then we applied GERMLINE with min_m = 1 and the results are stored in a match file.


---

## What Is Left

**GERMLINE Result Evaluation**: Find better ways to evaluate the results from GERMLINE, and to be comparable with results from plinks.

**Threshold Tuning**: Refine the classification thresholds (e.g., TH_FIRST = 0.25, TH_SECOND = 0.10 in GERMLINE) to better match the expected relationship distributions.

**Visualization**: Add scatter plots (e.g., Z0 vs. Z1 for PLINK results, or Total IBD vs. Max Segment Length for GERMLINE) to visually cluster the different relationship degrees.
